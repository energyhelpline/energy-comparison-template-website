@model BareboneUi.Common.ViewModelWithErrors

@if (Model.Errors.Any())
{
    <ul id="errors">
        @foreach (var error in Model.Errors)
        {
            <li id="error">@error</li>
        }
    </ul>
}